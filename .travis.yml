language: python
python:
  - "3.6.5"
install:
  - pip install -r requirements.txt
  - pip install coveralls codecov
script:
  - py.test test_redflags.py
  - coveralls
after-success:
  - coveralls
  - codecov --token
